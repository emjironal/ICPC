#include <iostream>
#include <iomanip>

using namespace std;

int main(){
	cin.tie(0);
	ios_base::sync_with_stdio(0);
	
	int escenarios, M ;
	cin >> escenarios;
	
	for (int i = 0; i < escenarios;i++)
	{
		cin>>M;
		
		int conciertos[M*3];
		
	}
	cout << resultado << "\n";

	return 0;
}
